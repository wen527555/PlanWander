// import TripModal from '@/components/TripModal';

// const EditTripModal: React.FC<{ tripData: TripData; onClose: () => void }> = ({ tripData, onClose }) => {
//   const handleUpdateTrip = async (tripTitle, startDate, endDate, selectedCountries) => {
//     await updateTrip(tripData.id, tripTitle, startDate, endDate, selectedCountries);
//   };
//   return <TripModal onClose={onClose} isEditing={true} initialData={tripData} onSubmit={handleUpdateTrip} />;
// };

// export default EditTripModal;
